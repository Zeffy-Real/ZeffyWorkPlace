# P1 · 编排闭环（多 Agent 协作 + 上下文压缩） 施工文档

> 所属：Zeffy-Workplace 作品集 MVP
> 上游输入：`P0变更摘要.md`（遗留缺口、关键设计决策、风险警示）、`MVP任务文档.md` P1 阶段、`P0施工文档.md`
> P1 目标：**替换 P0 的 echo handler 为真实 Agent 分发，用【自研 WorkflowEngine + TaskNode】打通「人类发任务 → Supervisor 拆解 → 领域 Agent 执行 → Reviewer 评审 → 人类验收」的端到端闭环**，并落地上下文压缩。
> 状态：✅ 已吸收最高强度深度审查（含 🔴 6 项必须修改 / ⭐ 5 项强烈建议），待审查确认后可编码
> 版本：v0.2（审查迭代）

---

## 〇、P1 继承与前置约束（摘自 P0 变更摘要，P1 必须遵守）

| 继承项 | 说明 |
|---|---|
| WS 协议 `wsmessage.py` | 复用 kind 枚举；P1 扩展：`agent_message`、`task_node_update`、`review_event` |
| LLMClient 签名 + 异常 | 复用 `agenerate(messages)->(text,usage)` + 三层异常 |
| `safe_resolve_workspace_path` | Agent 文件工具唯一入口，禁止拼接路径 |
| 后台任务缺口 | ⚠️ **Agent 长任务禁止在 WS 协程直接跑** |
| uvicorn `--workers 1` | P1 仍强制单 worker（内存会话） |
| create_all | P1 **第一件事接入 alembic 迁移**，废弃 create_all |
| 内存 SQLite 测试 | 单测不依赖 Docker PG |

**P1 交付方式说明（重要取舍）**：MVP 主文档已确定 P1 采用「同步图 + HTTP 内 checkpoint」，异步化（Celery/Redis）放到 P2。因此 P1：
- 任务执行走 **独立 `TaskRunner` 后台任务协程**，WS 只接收触发信号 + 推送状态，**避免 WS 协程跑长任务**；

---

## 〇.1 架构真相源约束（🔴 最高风险点，P1 强制约定）

> 这是审查列出的最高风险架构点。P1 必须锁死，否则后期排错成本爆炸。

| 约束 | 内容 | 违反后果 |
|---|---|---|
| **业务唯一真相源 = 自研 `WorkflowEngine` + PG `TaskNode` 表** | 任务流转、节点状态、验收结论全部以 PG 为准；业务层真相源只有一个 | 状态双源冲突，任务卡死 |
| **LangGraph 仅作为 Agent 内部执行图** | LangGraph checkpoint 只是 Agent 单次执行的断点快照，**不承担业务流转、不做任务真相源** | 业务 TaskNode `running` 而 LangGraph 内部已失败/中断，状态分裂 |
| **禁止 LangGraph 驱动 TaskNode 状态变更** | TaskNode 状态只允许由 `WorkflowEngine.advance()` 修改 | 非法状态跃迁、并发写损坏 |
| **`thread_id = task.uuid`** | 每个 Task 唯一 `thread_id`，禁止全局共用；否则不同任务状态互相覆盖污染（写入 P1-3 AgentRunner 注释） | 多任务串数据 |
| **WS 只做事件推送，不做状态可信源** | 前端每次刷新必须拉取 PG 业务数据，不依赖 WS 内存状态；WS 消息仅作即时提醒 | 前端显示与事实不符 |

---

## 〇.2 全局硬预算与背压（⭐ 强烈建议，规避死循环 / 429 协程堆积）

- **每个 Task 全局最大 Agent 轮次 `MAX_TASK_ROUNDS`**：超出即任务置 `failed` 并写 AuditLog，升级人工介入。**不依赖任何单一 Agent 自我约束**。
- **LLM 调用信号量 `asyncio.Semaphore`**：限制最大并发 LLM 调用，作为对 LLM 429 限流的背压，防 event-loop 饥饿、协程无限堆积。
  - P1-1 预埋信号量壳，P1-3 在 Agent 调用处接入。
- 可重试异常（网络 / 429 限流）→ 有限次数指数退避重试；不可恢复异常（配置错误 / 参数非法）→ 直接 `failed` + 审计 + 停止流转。

---

## 一、P1 总目标与总验收（对应 MVP 主文档 A1/A2/A4/A5/A6）

| # | 验收点 | 门槛 |
|---|---|---|
| A1 | 端到端闭环 | 提交「写一份 PRD」任务 → 零人工跑到验收卡，产物写入 workspace（`task-<id>/prd.md`）**且校验文件内容非空、非占位** |
| A2 | 多 Agent 协作 | 触发一次「评审打回 → 领域 Agent 修正 → 再评审通过」回流；并验证超最大修订次数 → 任务终止 |
| A4 | HITL 决策 | 触发一次 `interrupt` 审批；人类驳回后 Agent 修正重试或终止；人类通过则继续 |
| A5 | 追问 | 信息不足时出追问卡（`review_event`），人类补充后从断点继续，上下文不丢 |
| A6 | 上下文压缩 | 跑满 `CONTEXT_MAX_ROUNDS` 后自动摘要；压缩前后 token 统计下降达标；收尾节点仍成功；**DB Message 原始记录未被动过** |
| A1-额外 | alembic 建表 | `alembic upgrade head` 一遍建表成功；废弃 create_all |

---

## 二、P1 拆分结构（5 个小阶段，各自独立可验收）

```
P1-1 基建升级：alembic + LangGraph 依赖 + 后台任务抽象（TaskRunner 壳 + 全局背压预埋）
P1-2 工作流引擎：workflow 状态机 + TaskNode 流转 + 模板（含乐观锁）
P1-3 多 Agent：supervisor/domain/reviewer + 工具注册表 + AgentRunner 接线 WS
P1-4 上下文压缩 + 记忆雏形：compressor（视图层）+ 会话记忆（短期）
P1-5 端到端对接 + HITL/追问 + 总验收（A1/A2/A4/A5/A6）
```

> 拆分原则：每个小阶段能「跑起来 + 看得到产物 + 有完成标准」，与 P0 风格一致。

---

## 三、P1-1 基建升级（alembic + LangGraph + TaskRunner + 背压）

**目标**：迁移体系就位、LangGraph 依赖可用、后台任务抽象有壳子（解决「WS 不能跑长任务」）、全局背压预埋。

**新增/修改文件**
- 修改 `server/pyproject.toml`：新增 `langgraph>=0.2`、`alembic>=1.13`、`langgraph-checkpoint-sqlalchemy`。
- `server/alembic.ini` + `server/migrations/`：alembic 初始化，`env.py` 读取 `DATABASE_URL`（异步 engine）。
- `server/app/db/models.py`：**保持 P0 字段不变**，生成首个迁移；落地升级/降级。
- 删除/停用 `init_db.py` 的 `create_all` 路径（lifespan 改为执行 `alembic upgrade head` 或提示手动执行）。
- `server/app/tasks.py`：`TaskRunner` 后台任务抽象。
  - `submit(run_id, task_db_id, fn, meta)`：投递到 in-process 后台任务；**记录 run_id/task_db_id/meta**。
  - `subscribe(run_id, ws_router)`：任务进度回调 → WS 推送。
  - **重启丢失任务风险声明（🔴 必须修改，见下）**。
  - `shutdown()`：**在 lifespan shutdown 收集全部运行中 task 并 `cancel()` + `await`**，避免 `Task was destroyed but it is pending` 与协程泄漏。
- `server/app/appstate.py`（新增）：预埋 `LLM_SEMAPHORE = asyncio.Semaphore(MAX_CONCURRENT_LLM)` 壳。

**🔴 alembic autogenerate 警告（必须修改）**
- `autogenerate` 无法识别枚举变化、`server_default`、列重命名 → **生成的迁移脚本必须人工逐行核对后才能执行，禁止直接 upgrade**。
- 数据路径二选一：
  - 全新环境：直接 `alembic upgrade head`。
  - 本地已有 P0 数据：**禁止直接 autogenerate 后运行**；开发人员选择清理 PG volume，或手动调整迁移脚本对齐存量数据。

**🔴 TaskRunner 进程内存任务限制（必须修改）**
- `asyncio.create_task` 是**进程内内存任务，无持久化**。
- **服务重启 / OOM / Ctrl-C → 所有运行中、等待 HITL 中断的任务全部丢失，状态无法恢复**。
- 没有持久化队列、没有任务持久记录、没有重试、没有死信。
- `tasks.py` 文件头用大字体注释高亮该限制；`TaskRunner.shutdown()` 必须 cancel+await 全部运行 task；验收文档写明：**P1 不支持服务重启后恢复运行中/等待审批任务，该能力归 P2**。

**⭐ TaskRunner 接口补强（强烈建议）**
- 元数据：`run_id`、`task_db_id`、状态回调、异常捕获。
- **所有未捕获异常必须落库到 AuditLog，任务状态置 `failed`，禁止仅打日志**。
- TaskRunner 不实现复杂重试；只做「捕获异常 + 写审计 + 更新 Task 状态」；重试归属 WorkflowEngine/AgentRunner 层。

**关键操作**
1. `uv add langgraph alembic langgraph-checkpoint-sqlalchemy`
2. `alembic init migrations`，env.py 用 async engine。
3. 首个迁移**人工核对后** `alembic upgrade head` 验证 5 表（替代 create_all）。
4. lifespan：init_db → `alembic upgrade head`；`TaskRunner.shutdown()` 挂 lifespan。
5. `TaskRunner` 最小可跑：提交 `async def demo()`，WS 收完成通知。

**产物**：alembic 迁移体系 + LangGraph 依赖 + TaskRunner 壳（含 shutdown）+ 背压信号量壳

**完成标准**：`alembic upgrade head` 建表成功；TaskRunner 提交 demo 并经 WS 收 `task_node_update`；shutdown 场景 cancel 行为有测试（`test_task_runner_shutdown`）。

---

## 四、P1-2 工作流引擎（状态机 + TaskNode 流转 + 模板）—— ✅ 已完成 2026-09-15

**实机验证**：`POST /advance` 走通 lightweight 全流程（需求→拆解→执行→验收），任务收尾 `done`；重复推进已 `done` 节点被拦截（400 非法迁移）；`pytest` 34 passed / 1 skipped。

**目标**：任务从「待分配」按工作流节点推进，每个节点落 TaskNode，非法流转被拦截。

**新增文件**
- `server/app/workflow/__init__.py`
- `server/app/workflow/templates.py`：内置 `generic`（需求→文档→设计→实现→评审→验收）与 `lightweight`（需求→拆解→执行→验收）。
  - 每个节点：`name / role / type(自动|人间|HITL)`。
  - **模板级配置 `max_revision` / `max_rounds`（⭐）**：避免硬编码写死在 AgentRunner；`generic` 内置评审最大修订次数，`lightweight` 可不同。
- `server/app/workflow/state_machine.py`：TaskNode 状态机
  - 合法迁移：`pending→running→done|failed|blocked`；`done→已验收`（通过验收）。
  - 非法迁移抛 `WorkflowStateError`。
- `server/app/workflow/engine.py`：`WorkflowEngine`
  - `start(task)`：创建全部 TaskNode，跑首节点。
  - `advance(task, node_id, result)`：校验迁移 → 写 output → 推进下一节点 / 收尾。
  - **乐观并发保护（⭐）**：更新 TaskNode 用行级乐观锁 `UPDATE ... WHERE id=? AND status=预期旧态`，防止并发多次 `advance` 竞态跃迁。
  - P1 只做简单顺序流转。**DAG 并行依赖、多节点并行属于 P2**，文档标注防过度开发。

**关键操作**
1. 模板定义（含 `max_revision`/`max_rounds`）。
2. 状态机 + 非法迁移抛错。
3. engine 用 repo 落 TaskNode，状态推进可回放；`advance` 带乐观锁。
4. 测试：`test_state_machine`（合法/非法迁移）、`test_engine`（走通 lightweight 全流程）、**`test_engine_optimistic_lock`（并发 advance 冲突）**。
5. 临时 `POST /tasks/{id}/advance`（开发调试用）。**权限说明（⭐）**：仅本地调试；P1-5 完成后禁用或加调试开关；**禁止前端不受限调用该接口绕过 Agent 流转**。

**产物**：工作流模板 + 状态机 + engine（含乐观锁）

**完成标准**：`pytest` 状态机/engine/乐观锁测试通过；`POST /tasks/{id}/advance` 能把 lightweight 推到验收；非法迁移被拦截。

---

## 五、P1-3 多 Agent（supervisor / domain / reviewer + 工具 + Agent Runner）—— ✅ 已完成 2026-09-15

**实机验证**：mock LLM 下 generic 工作流由 AgentRunner 走通「需求分析→文档→设计→实现→评审」，并中断于「验收(人审)」；WS 实测收到 `task_update(running)`→`task_node_update(需求 failed, LLMConfigError)`→`task_update(done)`（无 key 失败→审计→推送契约成立）；fs 白名单＋幂等（no_overwrite 拒覆写）；`pytest` 全量 **55 passed / 1 skipped**。

**目标**：把 P0 echo handler 替换为真实 Agent 分发，多 Agent 协作可跑。**P1 复杂度最高模块。**

**新增文件**
- `server/app/agents/__init__.py`
- `server/app/agents/base.py`：`BaseAgent` + `AgentResult`（status/文本/产物路径/usage/决策点）。统一元数据、标准输入输出、状态回调。**Result 完整含 usage / 异常 / 决策**。
- `server/app/agents/supervisor.py`：`SupervisorAgent`
  - 理解任务 → 拆解为子步骤（≤2 层）→ 分配角色 → 返回 `PlanResult`。
  - 行为范式：Plan-and-Execute。
  - **拆解输出强制附带每个子任务验收标准（⭐）**：prompt + 输出 schema 强制要求 `acceptance_criteria`，否则评审 Agent 无依据。
- `server/app/agents/domain.py`：`DomainAgent`（文档/设计/代码等，按模板 role 实例化）
  - 用 LLM 生成产物 → 可选调用工具（写文件）。
  - 行为范式：**P1 只做单轮 ReAct（直调），完整多步 ReAct 循环放 P2**；文档标注防超范围开发。
- `server/app/agents/reviewer.py`：`ReviewerAgent`
  - 评审产物 → 返回 pass/revise 结论 + 意见。行为范式：Reflection。
- `server/app/agents/runner.py`：`AgentRunner`
  - 按节点交权给对应 Agent；结果回写 TaskNode；触发下一节点；`interrupt` 挂起等人工。
  - **LangGraph `interrupt()` 副作用重执行坑（🔴 见下）**。
  - 异常分层处理（🔴 见下）。
- `server/app/tools/registry.py` + `server/app/tools/fs.py`：
  - 注册表：name / 参数 schema / 权限等级 / 适用场景；**每个工具定义超时时间、最大调用次数（⭐）**，单任务内超限直接阻断。
  - `fs` 工具：**必须走 `safe_resolve_workspace_path`**，写 `task-<id>/`。
- `server/app/wsmessage.py`：扩展 kind：`agent_message`、`task_node_update`、`review_event`。
- 修改 `server/app/api/ws.py`：`_echo_handler` → `_dispatch_handler`；**入参用 pydantic schema 校验（⭐）**，非法 kind / 缺失字段直接丢弃并记审计，不透传业务逻辑。

**🔴 LangGraph interrupt() HITL 副作用重执行坑（必须修改）**
- LangGraph 在 `interrupt()` 之后 resume 时，**会从该 node 开头重新完整执行**，不是从 interrupt 断点继续。
- ⚠️ 若 interrupt 之前已有副作用（写文件 / 外部工具），resume 会重复执行 → 重复产物、重复 IO。
- P1 强制落地约束（写入文档 + AgentRunner 代码注释）：
  1. **所有带副作用的逻辑（fs 写文件、外部工具调用）必须放在 `interrupt()` 调用之后**，不能放前面。
  2. 若无法避免，工具必须携带幂等 key；fs 写文件先做存在判断或用「临时文件 + rename」模式。
- **fs 幂等防护（🔴）**：多次执行同一 Agent 会重复输出同一文件。写文件工具支持 覆盖/不覆盖/临时文件 模式，**默认不盲目覆盖**；或输出文件名带 `run-id` 后缀。

**🔴 AgentRunner 异常分层（必须修改）**
- 区分：`LLMConfigError / LLMConnectionError / LLMProviderError / WorkflowStateError`。
  - 可重试（网络、429 限流）：WorkflowEngine 配置有限次数指数退避重试。
  - 不可恢复（配置错误、参数非法）：TaskNode 置 `failed` + 写审计 + 停止流转，**禁止静默吞异常**。
- **所有 Agent 执行（入参 / 输出 / usage / 异常 / 决策）完整写入 AuditLog**，不能只打日志。

**关键操作**
1. 实现 base/domain/supervisor/reviewer 最小 LLM 调用（`LLMClient.agenerate`，经信号量限流）。
2. `AgentRunner` 接 `WorkflowEngine`：节点→Agent→结果→推进。
3. 工具注册表（含超时/次数）+ `fs`（白名单 + 幂等）。
4. ws.py：扩展 kind，接 TaskRunner + AgentRunner，**不在 WS 协程跑 LLM**。
5. 测试：`test_agents_plan`（拆解含验收标准）、`test_tools_fs`（写文件 + 路径越界拒绝 + 幂等不盲目覆盖）、`test_runner_dispatch`（mock LLM 走通节点）、`test_runner_exceptions`（可重试 / 不可恢复异常两路径 + 审计落库）。

**产物**：多 Agent 角色 + 工具注册表 + AgentRunner + WS 接线

**完成标准**：mock LLM 下一条任务走到「评审」节点；fs 走白名单且不重复覆盖；`agent_message`/`task_node_update` 经 WS 推送；异常走审计落库。

---

## 六、P1-4 上下文压缩 + 会话记忆（短期）——视图层压缩 —— ✅ 已完成 2026-09-15

**实机验证**：`should_compress` 双重阈值（轮次 OR token）成立；`compress` 产出摘要保留白名单字段且 token 下降；LLM 摘要失败降级截断（degraded=True，保留最近 10 条）不使任务失败；压缩视图可继续喂给收尾 LLM；🔴 **DB Message 原始记录压缩后一条未变**。`pytest` 全量 **62 passed / 1 skipped**。

**目标**：A6 达成——长对话自动摘要压缩，收尾仍能干活；短期会话记忆雏形。

**🔴 原始 Message 记录禁止删除（必须修改）**
- **压缩只做「LLM 输入视图层变换」，永久保留原始记录**。
- 正确策略：
  - 原始消息**完整保留在 DB Message 表**；
  - 压缩 = 传给模型的**上下文视图** = `摘要 + 最近 N 条未压缩消息`；
  - **数据库原始记录永远不动**。
- ❗ 禁止修改 / 删除 DB Message 表数据；压缩属运行时视图，不是持久层变更。

**新增文件**
- `server/app/memory/__init__.py`
- `server/app/memory/store.py`：短期会话记忆（任务级 messages 缓存；视图 = 最近 N 条 + 压缩摘要）。
- `server/app/memory/compressor.py`：`ContextCompressor`
  - `should_compress(rounds, token_est)`：**双重条件（轮次阈值 OR token 阈值）任一满足即触发**；token 用保守上界估算，不盲信 LLM usage。
  - `compress(history, decisions) -> CompressedView`：生成摘要，**保留白名单字段（task 目标 / 已完成节点 / 评审结论 / 重大人类决策 / 失败原因）**；返回 + token 统计（复用 LLMClient）。
  - **压缩失败降级（⭐）**：LLM 摘要失败（限流/报错）不使任务失败；降级为「截断丢弃最老非关键消息」继续运行，写审计标记压缩失败。

**关键操作**
1. `compressor` 摘要 prompt 模板化（版本化入口，P3 接 Langfuse）。
2. `store` 任务上下文 = 最近 N 条 + 压缩摘要（LRU-like）。
3. `SupervisorAgent` 拆解、`DomainAgent` 生成时注入压缩后视图。
4. 测试：`test_compressor`（超阈值产出、保留白名单、token 下降达标、**压缩失败降级路径**）、`test_memory_store`（注入 + 读取）、**校验 DB Message 原始记录不变**。

**产物**：compressor（视图层）+ 短期记忆 + 测试

**完成标准**：mock LLM 下超阈值触发摘要，含 decision/node；**DB Message 未被删除**；`test_compressor` 通过。

---

## 七、P1-5 端到端对接 + HITL/追问 + 总验收

**目标**：A1/A2/A4/A5/A6 全绿，人工可走完整闭环。

**🔴 识别两类 interrupt（必须修改）**
- **HITL 审批**、**信息不足追问**：两套业务逻辑，分开处理。
- 都受 P1「重启丢失任务」限制。
- 验收用例覆盖：追问后继续执行、审批驳回修正、审批通过、超最大修订次数终止。

**🔴 产物落盘校验（必须修改）**
- 手工验收 A1 必须校验产物确实写入 `task-<id>/prd.md`，**且校验文件内容非空、非占位**，不只判断目录存在。

**关键操作**
1. `SupervisorAgent` 完整拆解（≤2 层 + 验收标准），`WorkflowEngine` 用模板建节点。
2. `AgentRunner` 全链路：拆解→领域 Agent→reviewer 回流（A2：控制最大修订次数，如 2 次）。
3. HITL（A4）：`interrupt` → WS 发 `review_event` 审批卡 → 等人工（同意/驳回/意见）→ resume；**副作用放 interrupt 之后**。
4. 追问（A5）：信息不足 → `review_event` ask 卡 → 人工补充 → 断点继续（context 用 store 保持不丢）。
5. 压缩（A6）：长任务自动触发；收尾节点用摘要视图仍成功；**DB 原始记录不动**。
6. 产物落盘：最终产物写 `task-<id>/`（fs + workspace，幂等）。
7. 收尾：任务 → `已验收`；WS 推 `task_node_update` 给验收卡。

**⭐ 可复现手工验收步骤（强烈建议，给测试者明确输入指令）**
- A1：提交「写一份 PRD，输出到 prd.md」，零人工到验收卡；人工开 `task-<id>/prd.md` 校验内容非空。
- A2 回流：提交任务并构造低质量产物，令 Reviewer 返回 revise；观测回流；继续触发到最大修订次数 → 观测任务终止。
- A4 审批：任务含 HITL 节点，人工先「驳回+意见」→ 观测修正重试；再「通过」→ 继续。
- A5 追问：提交信息不足任务（如缺需求字段），观测 ask 卡；人工补充后断点继续，上下文不丢。
- A6 压缩：构造长对话跑满阈值，观测压缩触发 + 收尾成功；抽查 DB Message 原始记录未删。

**完成标准（手工验收 A1~A6）**
- A1 提交 PRD 任务零人工到验收卡，`task-<id>/prd.md` 内容有效。
- A2 评审打回→修正→通过 + 超修订次数终止。
- A4 interrupt 审批（驳回修正 + 通过）。
- A5 追问 + 断点继续。
- A6 压缩触发 + 收尾成功 + 原始记录保留。

**端到端测试必须双跑（⭐）**：mock-LLM 自动化集成测试 + 真实 LLM 手工验收。

---

## 八、P1 文件清单总览（相对 P0 增量）

```
server/
│   ├── alembic.ini + migrations/          # P1-1
│   ├── app/
│   │   ├── tasks.py                        # P1-1 TaskRunner（in-process 壳，重启丢失风险注释）
│   │   ├── appstate.py                     # P1-1 LLM_SEMAPHORE 背压壳
│   │   ├── workflow/
│   │   │   ├── templates.py / state_machine.py / engine.py   # P1-2（含乐观锁）
│   │   ├── agents/
│   │   │   ├── base.py / supervisor.py / domain.py / reviewer.py / runner.py  # P1-3
│   │   ├── tools/
│   │   │   ├── registry.py / fs.py         # P1-3（幂等 + 超时 + 次数）
│   │   ├── memory/
│   │   │   ├── store.py / compressor.py    # P1-4（视图层压缩）
│   │   ├── wsmessage.py                    # 扩展 kind（P1-3）
│   │   └── api/ws.py                       # echo→dispatch + 入参校验（P1-3）
└── tests/
    ├── test_state_machine.py / test_engine.py / test_engine_optimistic_lock.py  # P1-2
    ├── test_task_runner_shutdown.py        # P1-1
    ├── test_agents_plan.py / test_tools_fs.py / test_runner_dispatch.py / test_runner_exceptions.py  # P1-3
    └── test_compressor.py / test_memory_store.py  # P1-4
```

---

## 九、P1 风险与回滚（含审查新增）

| 风险 | 影响 | 预案/回滚 |
|---|---|---|
| **TaskRunner 内存协程，重启丢失运行中/HITL 任务** | P1 任务全部丢失 | 文档+注释明确限制；**P2 引入持久任务队列**；开发测试避免重启服务测长任务 |
| **LangGraph interrupt resume 重执行 node，副作用重复** | 重复写文件、重复调工具 | 强制副作用放 interrupt 之后；工具幂等；fs 防重复覆盖；写入 AgentRunner 注释 |
| **双状态源（TaskNode + LangGraph checkpoint）不一致** | 业务状态与 Agent 内部状态分裂，任务卡死 | 约定 PG TaskNode 唯一真相源；LangGraph checkpoint 仅 Agent 内部快照；禁止 LangGraph 改 TaskNode |
| LLM 无背压，并发 429 限流协程堆积 event-loop 饥饿 | 响应变慢、任务超时 | P1-1 预埋 `asyncio.Semaphore`；指数退避重试；超限任务标 failed |
| 评审死循环（不断打回） | 任务卡死 | 模板级 `max_revision`/`max_rounds`；超限升级人工或终止 |
| 上下文压缩误伤决策 | 收尾质量下降 | 白名单保留 decision/node/conclusion；压缩后带人工复核点 |
| **上下文压缩调用失败（LLM 限流）→ 整任务失败** | 正常任务意外终止 | 降级策略：放弃摘要，截断老消息继续，写审计标记压缩失败 |
| alembic autogenerate 盲区 | 字段丢失、数据损坏 | 迁移脚本**人工审核**后才能执行；改模型优先清 volume；禁止上线未审核迁移 |
| 处理中服务重启 | 协程 pending + 泄漏 | `TaskRunner.shutdown()` cancel+await 全部运行 task |
| Agent 重复写同一文件 | 产物版本混乱 | fs 覆盖/不覆盖/临时文件模式；默认不盲目覆盖；文件名带 run-id |

---

## 十、P1 测试策略

- **单元**（mock LLM + FakeLLMClient，内存 SQLite，含信号量注入）：
  - 状态机、engine（含**乐观锁并发冲突**）、supervisor 拆解（含验收标准）、compressor（含**压缩失败降级**）、tools_fs（含幂等）、memory、**TaskRunner shutdown cancel**、**Runner 异常两路径**。
- **集成**（mock-LLM 自动化 + 可选真 LLM 冒烟）：`POST /tasks` → 后台跑 → WS 收 `task_node_update` 到验收。
- **手工验收**：A1~A6 可复现步骤（第七节）。
- **压测标注**：⚠️ **P1 不做并发多任务压测；多任务并发归 P2**；P1 验证单任务全链路闭环。

---

## 十一、P1 实现顺序（审查通过后执行）

1. **P1-1** alembic（人工审核迁移）+ LangGraph 依赖 + TaskRunner 壳（shutdown）+ 背压信号量
2. **P1-2** workflow 模板/状态机/engine（乐观锁）
3. **P1-3** agents + tools（幂等/超时/次数）+ WS dispatch 接线
4. **P1-4** compressor（视图层）+ 短期记忆
5. **P1-5** 端到端对接（回流/HITL/追问/压缩）+ A1~A6 手工验收
6. 产出 **P1 变更摘要**（沿用 P0 模板 + **P1 已知硬限制清单**，见下），并给出 **P2 拆解版施工文档**

---

## 十二、P1 已知硬限制（写进 P1 变更摘要，作为 P2 施工文档输入）

> 审查要求：P1 变更摘要除 P0 模板外，**必须**增加本清单，逐项成为 P2 输入。

1. **TaskRunner 为进程内内存任务，重启丢失运行中任务 / HITL 等待任务**（P2：持久任务队列）。
2. **LangGraph 仅 Agent 内部执行；业务真相源为 PG TaskNode**（约束持续有效）。
3. **P1 不支持 DAG 并行工作流**（仅顺序流转）。
4. **无向量库记忆，仅短期任务级内存记忆**（P2：中长期 + Qdrant）。
5. **无完整多步 ReAct 循环**（仅单轮直调）。
6. **不支持多实例部署**；WS 连接内存存储，强制 `--workers 1`。
7. **用户鉴权 / 多用户不存在**；单用户本地模式。

**✨ 可选优化（记入 P1 遗留缺口，P2 实现）**
- 工具调用完整 metrics 统计；
- 更完善的循环检测逻辑；
- 真正的流式 Agent 输出。

---

## 十三、与 MVP 主文档的一致性

本 P1 严格对齐 MVP 任务文档的 P1 阶段（A1/A2/A4/A5/A6）与 P2 前置约束（异步化放 P2、剥 LangGraph 为 Agent 内壳）。收敛取舍（自研状态机作真相源、LangGraph 仅 Agent 内执行）**不改变 A1~A6 验收标准**。