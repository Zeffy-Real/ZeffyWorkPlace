# Zeffy-Workplace · P2 审查总结报告

> 日期：2026-09-15
> 范围：P2 持久化任务队列（ARQ）+ 断点恢复 + lease 巡检 + DAG 建模 + 前端接入
> 审查强度：最高强度深度审查（分布式一致性 / 幂等 / 可靠性 / 故障恢复）

---

## 一、结论概览

先给结论：**P2 的可上线性审查已通过，全部 8 项「🔴 必须修改」均已落地实现并实机验证**。

| 维度 | 结论 |
|---|---|
| 架构承接 | ✅ P1 约束（PG TaskNode 唯一真相源、内存不驱动业务）全程保持 |
| 一致性 | ✅ DB 优先入队 + 补偿回滚 + 巡检兜底，最终一致 |
| 幂等 | ✅ 三层防护（队列 job_id / DB 乐观锁 / 业务 fs） |
| 可靠性 | ✅ ARQ 悲观执行 + 断点恢复白名单 + lease 死任务回收 |
| 前端 | ✅ 入队确认 + 事件保序 + 节点/审批/追问卡 + DB 对账重建 |
| 测试 | ✅ `pytest` 89 passed / 1 skipped；ruff 通过；前端 build 通过；真实 LLM 双进程实机验证通过 |

---

## 二、审查「🔴 必须修改」逐条状态

### 1. 入队与 DB 状态一致性（分布式事务缺失）—— ✅
- **问题**：无分布式事务，"先入队/先更 DB"各有致命缺陷。
- **修复**（[gateway.py](file:///d:/TraeProjects/A-ZeffyWorkPlace/zeffy-workplace/server/app/queue/gateway.py) + [recovery.py](file:///d:/TraeProjects/A-ZeffyWorkPlace/zeffy-workplace/server/app/queue/recovery.py)）：
  1. **DB 优先**：先 `pending→queued`（DB 原子更新 + commit），再入队 ARQ；
  2. **失败补偿**：入队异常 → 回滚 `queued→pending` + 写 `enqueue_failed` 审计 + 抛错转前端通知；
  3. **巡检兜底**：`queued` 滞留超 `QUEUED_STALE_SECONDS`(60s) 仍未认领 → 重新入队（job 从 Redis 丢失时兜底重投），ARQ `_job_id` 幂等保证不重复执行。
- **验证**：`test_queued_fresh_not_re_enqueued` / `test_queued_stale_re_enqueued`。

### 2. Lease 死任务检测机制缺失 —— ✅
- **问题**：worker OOM / kill -9 时 lease 不释放，任务永久卡 running；且任务级 job 的**后续节点 running 无 lease**（盲区）。
- **修复**（[arqs.py](file:///d:/TraeProjects/A-ZeffyWorkPlace/zeffy-workplace/server/app/queue/arqs.py) + [runner.py](file:///d:/TraeProjects/A-ZeffyWorkPlace/zeffy-workplace/server/app/agents/runner.py) + [repos.py](file:///d:/TraeProjects/A-ZeffyWorkPlace/zeffy-workplace/server/app/db/repos.py)）：
  1. **每节点续约**：`AgentRunner.lease_renewer` 钩子，worker 每轮对 active running 节点 `renew_node_lease`（仅 running 命中的乐观续约）——消除盲区；
  2. **巡检回收两类死任务**：lease 显式过期；或用 `updated_at` + grace（ARQ_JOB_TIMEOUT×1.5）兜住"worker 永久死亡、无任何 lease 痕迹"的节点；
  3. HITL 等待节点不回收（人工 resume）。
- **验证**：`test_running_expired_lease_recovered` / `test_running_without_lease_stale_recovered` / `test_running_without_lease_fresh_skipped`；实机 `worker_id_touched=True`。

### 3. 断点恢复场景全覆盖 + 状态白名单 —— ✅
- **问题**：queued 重启重复 / 执行一半副作用重跑 / failed-done 误恢复。
- **修复**（[recovery.py](file:///d:/TraeProjects/A-ZeffyWorkPlace/zeffy-workplace/server/app/queue/recovery.py)）：
  - **白名单**：仅 `queued`（滞留超时）与 lease 过期/超时的 auto `running` 重入队；`done/failed/blocked/pending` 一律不动；
  - **HITL 持久化 blocked**：审批挂起 = blocked，纳白名单天然跳过（见第 7 项）；
  - **死信**：`attempts ≥ ARQ_MAX_TRIES` 仍滞留 → failed + 审计，终止无限重试。
- **验证**：`test_skip_done_task` / `test_queued_dead_letter_when_attempts_exceeded` / `test_skip_running_without_lease`。

### 4. 三层幂等防护 —— ✅
- **问题**：ARQ"至少一次投递"，重传/崩溃重连多次执行。
- **修复**：
  1. **队列层**：`_job_id`（普通=task_id / resume=resume-<task>-<node>）去重；
  2. **DB 层**：`claim_node` 原子 `UPDATE ... WHERE status='queued'` 乐观锁，单节点同时仅一个 worker 能 claim；
  3. **业务层**：fs 工具沿用"存在判断/临时文件+rename"幂等约定，重跑不重复覆盖。
- **验证**：`test_claim_idempotent`。

### 5. WS 事件可靠性 / 乱序 —— ✅
- **问题**：Pub/Sub 发后即忘、网络乱序、入队无确认。
- **修复**（[events.py](file:///d:/TraeProjects/A-ZeffyWorkPlace/zeffy-workplace/server/app/queue/events.py)）：
  - 每事件带 **per-task 单调 seq**（Redis INCR，跨 run/resume job 不重置）+ 时间戳；
  - API 侧 `EventSequencer` 按 (task_id, seq) 排序去重，乱序/重复丢弃；
  - **入队确认**：`enqueue`→`task_update(event=enqueued)` 回前端才更新 UI。
- **验证**：实机端到端入队确认 → 节点事件 → 验收卡链路；前端 REST 对账兜底补缺口。

### 6. DAG 完整成环检测 —— ✅
- **问题**：只能检测直接自引用，间接成环（A→B→C→A）漏检。
- **修复**（[engine.py](file:///d:/TraeProjects/A-ZeffyWorkPlace/zeffy-workplace/server/app/workflow/engine.py) `_validate_dag`）：完整拓扑排序，未知依赖/自引用/重复节点/间接成环全部拦截；`_activate_next` 仅激活"依赖已 done"的就绪节点（保持单活串行）。
- **验证**：`test_topology_rejects_cycle/unknown/self/duplicate` + `test_activate_respects_depends_on`。

### 7. HITL 等待任务持久化 + 前端 DB 重建卡 —— ✅
- **问题**（P2-1 + P2-5 交界）：审批挂起态语义不清；前端刷新后等待审批/追问的卡看不到。
- **修复**：
  - 后端 [runner.py](file:///d:/TraeProjects/A-ZeffyWorkPlace/zeffy-workplace/server/app/agents/runner.py)：审批中断节点 **running→blocked 持久化**；通过时 blocked→running→done（乐观锁防并发），驳回时 blocked→pending 重走审批；
  - 前端（[App.tsx](file:///d:/TraeProjects/A-ZeffyWorkPlace/zeffy-workplace/client/src/App.tsx) + [protocol.ts](file:///d:/TraeProjects/A-ZeffyWorkPlace/zeffy-workplace/client/src/lib/protocol.ts)）：`NodeOut` 增 `node_type`，reconcile 发现 `blocked && hitl` 节点即**据 DB 重建审批卡**，不再依赖卡专用 WS 事件。
- **验证**：`test_runner_walks_generic_to_review_skipping_hitl`（验收=blocked）；实机：审批挂起验收=blocked → approve → 全 done → `/nodes` 返回 node_type=hitl。

---

## 三、审查「⭐强烈建议」采纳情况

| 建议 | 状态 |
|---|---|
| 明确单 worker 定位（不并发 claim） | ✅ `max_jobs=1`，P2 文档标注，多 worker 归 P3 |
| 全链路审计（入队/认领/执行/完成/失败/重试） | ✅ `worker_job_failed`/`dead_letter`/`enqueue_failed`/`agent_run` 等审计落库 |
| 模板级最大重试次数 | ✅ 用 `ARQ_MAX_TRIES` 全局配置 + 节点 `attempts`；模板 `max_revision/max_rounds` 已有 |
| `claim_node` 原子更新（禁先查后更） | ✅ 已实现 |
| 前端状态对账 / 节点视觉映射 / 进度条 | ✅ REST 对账 + 状态色板已实现；进度条记入遗留 |
| 事件通道按 task 隔离、订阅先于对账 | ✅ 按 task_id 路由 `_task_ws`；对账在事件后触发 |

## 四、遗留与 P3 预埋

- **单 worker、单 API 实例**：不支持水平扩展 / 多实例跨 worker 广播。
- **真多节点并行执行**：P2 仅 DAG 建模 + 串行。
- **队列监控 / 指标埋点 / 告警体系**：暂无（`/health` 有 DB/Redis 探测）。
- **多用户 / 权限体系**：个人工作台形态，暂无。
- **单页 demo 无任务路由**：前端审批卡 DB 重建由 WS 携带 task_id 后触发 reconcile，未做独立任务页导航。
- **事件后端重放**：Pub/Sub 发后即忘，断线遗失事件靠前端全量 REST 对账兜底（已文档化）。P3 若需强一致可换 Redis Stream 持久订阅。

---

## 五、验证证据汇总

- **单元/集成**：`pytest` **89 passed / 1 skipped**；`ruff check app tests` 通过。
- **前端**：`tsc -b && vite build` 通过（含 blocked 重建卡、node_type、sendDecision）。
- **实机双进程**（API 8787 + ARQ worker + 真实 DeepSeek）：
  - 完整闭环：提交 → enqueued → worker 执行 → 节点事件 → 验收卡 → 审批 → 全 done；
  - lease 续约生效：任务节点 `worker_id` 有值、审批挂起节点带 lease；
  - HITL blocked：验收节点 DB 持久化 blocked，`/nodes` 返回 `node_type=hitl`。

## 六、相关提交

- `c8b5848` P2 主体（持久队列/恢复/DAG/前端）
- `ac755f5` 一致性巡检闭环 + lease 死任务检测闭环（审查二轮 🔴）
- `d1ebf42` HITL blocked 持久化 + 前端 DB 重建卡（审查剩余 🔴）

---

*报告依据 `.trae/documents/P2施工规划.md` 的「最高强度深度审查」意见逐条结项；实现细节见各提交 diff 与相关文件。*