# Zeffy-Workplace 多智能体协作工作台 · 全方案深化设计
> 核心定位：**人类只负责「创意提出、重大决策、最终验收」，其余全链路由多 Agent 在群聊式界面中自主拆解、协作、执行、评审**的任务型生产力工作台。先落地 Web 端，架构原生兼容后续桌面端（Tauri/Electron）迁移。
> 设计原则：全程贯彻**组件化、插件化、可复用、可扩展、高并发、高安全**六大基准，所有模块接口化、可插拔、可替换。

---

## 一、产品核心价值与协作范式
### 核心协作流程（单群聊闭环）
```
人类发布 IDEA/任务
    ↓
【调度 Agent】接收 → 任务拆解 → 生成执行计划 → @对应角色 Agent
    ↓
各角色 Agent 认领任务 → 按工作流节点依次执行 → 互相 @ 交付中间产物
    ↓
关键节点自动触发【评审 Agent】审查 → 不通过打回修正 → 通过进入下一节点
    ↓
全链路完成 → 自动汇总产物 → 推送人类验收
    ↓
人类反馈 → 迭代 / 结项 → 沉淀技能与模板
```
- 人类全程仅在**创意输入、重大方案决策、最终交付验收**三个节点介入，中间过程零干预。
- 所有协作发生在**同一个群聊窗口**内，以「@提及 + 消息回复 + 任务卡片」为交互载体，无复杂侧边栏与多级菜单。

---

## 二、整体架构设计（插件化分层架构）
采用**六层插件化架构**，层间通过标准接口通信，任意模块可替换、可扩展、可复用，完全满足高维护性与拓展性要求。

| 层级 | 职责 | 核心特性 | 插件化支持 |
|---|---|---|---|
| **接入层** | 端侧入口、交互渲染 | Web 端优先、后续桌面端复用核心逻辑 | 多端接入插件 |
| **编排层** | 任务调度、工作流驱动、Agent 协调 | 异步任务队列、事件驱动、状态机流转 | 工作流模板插件、调度策略插件 |
| **Agent 层** | 角色化智能体、行为执行、推理决策 | 角色可插拔、行为范式可配置、自主协作 | Agent 角色插件、行为模式插件 |
| **记忆层** | 上下文管理、长期记忆、技能沉淀 | 分层记忆、向量检索、自动压缩与遗忘 | 记忆库插件、知识库插件 |
| **工具层** | 能力调用、外部系统对接 | 统一函数调用协议、权限隔离、幂等执行 | 工具集插件、第三方集成插件 |
| **基础设施层** | 并发、存储、安全、监控 | 高并发异步、数据持久化、全链路审计 | 存储插件、监控插件 |

### 架构核心优势
1. **结构清晰**：上下层依赖明确，无交叉调用，定位问题快，维护成本低。
2. **极致拓展**：新增 Agent、工具、工作流、端侧均无需改动核心代码，插即用。
3. **高并发性**：异步事件驱动 + 任务队列，支持上百个任务、数百个 Agent 并发执行互不阻塞。
4. **复用性强**：所有 Agent、工具、工作流模板均可沉淀复用，同类任务零重复开发。

---

## 三、多 Agent 角色体系（可插拔扩展）
采用「**1 个全局调度 + N 个角色执行 + 1 个评审仲裁**」的角色体系，所有角色遵循统一 Agent 接口规范，可随时新增/禁用角色。

### 1. 核心常驻 Agent
| 角色 | 定位 | 核心能力 | 行为模式 |
|---|---|---|---|
| **调度总管 Agent** | 任务入口与全局协调 | 任务拆解、计划生成、资源分配、异常兜底 | Plan-and-Execute |
| **评审 Agent** | 全链路质量把控 | 需求评审、产物审查、方案仲裁、风险识别 | ReAct + 反思模式 |
| **文档 Agent** | 文档产出与梳理 | 需求文档、方案文档、测试文档、总结沉淀 | Plan-and-Execute |

### 2. 领域执行 Agent（按需加载，插件化扩展）
- **设计 Agent**：UI 设计、交互方案、视觉规范、原型生成
- **前端 Agent**：页面代码实现、组件开发、样式还原、bug 修复
- **后端 Agent**：接口开发、数据库设计、逻辑实现、部署发布
- **测试 Agent**：用例编写、自动化测试、缺陷定位、回归验证
- **运维 Agent**：服务器配置、部署发布、监控告警、故障排查
- **数据 Agent**：数据分析、报表生成、指标计算、结论输出

### 3. Agent 标准化规范
所有 Agent 必须实现统一接口：
- 元数据：角色名、描述、适用场景、依赖工具、消耗等级
- 输入输出：标准任务格式输入、标准产物格式输出
- 状态回调：执行中、成功、失败、阻塞四种状态实时上报
- 异常处理：超时重试、错误降级、兜底机制

> 拓展性：新增领域 Agent 仅需实现标准接口 + 角色 Prompt，注册后即可被调度器自动发现与调用，无需修改核心编排逻辑。

---

## 四、任务编排与工作流引擎
### 1. 工作流模板体系
内置通用模板 + 自定义模板，支持可视化编排，每个任务绑定对应工作流。
- **通用模板**：需求→文档→设计→实现→评审→验收（标准产品研发流）
- **轻量模板**：需求→拆解→执行→验收（简单任务流）
- **评审模板**：执行→初审→修改→终审→交付（强质量把控流）
- **自定义模板**：用户可拖拽节点、配置角色、设置流转条件、配置审批节点

### 2. 任务拆解算法
- 拆解规则：基于任务类型匹配历史最佳拆解方案，结合大模型推理生成子任务；子任务满足「独立可交付、边界清晰、有明确验收标准」。
- 粒度控制：自动控制拆解层级，复杂任务最多拆解 3 层，避免过度拆解。
- 依赖分析：自动识别子任务间的依赖关系，生成有向无环图，并行执行无依赖任务。

### 3. 异步任务编排机制
- 基于事件驱动 + 消息队列，所有任务异步执行，不阻塞主会话。
- 任务状态机：待分配→执行中→评审中→已完成→已验收 / 失败 / 阻塞，全状态可追溯。
- 调度策略：支持优先级调度、先来先服务、资源均衡三种模式；高优任务插队执行。
- 异常兜底：任务失败自动重试（默认 2 次），重试失败升级人类介入；超时自动熔断，释放资源。

### 4. 群聊协作协议
- **@ 触发机制**：消息中 `@角色名` 自动定向分发任务给对应 Agent，支持 @多个 Agent 并行执行。
- **交付机制**：Agent 完成任务后，在群聊中输出产物卡片并 @下一节点 Agent / 调度总管。
- **追问机制**：信息不足时，Agent 可 @提问对象补充信息，暂停任务等待回复。
- **冲突仲裁**：多 Agent 意见分歧时，自动触发评审 Agent 仲裁，输出结论后继续执行。

---

## 五、分层记忆系统与上下文工程
### 1. 三级记忆架构
| 记忆层级 | 存储内容 | 生命周期 | 检索方式 | 作用 |
|---|---|---|---|---|
| **短期会话记忆** | 当前群聊对话、任务上下文 | 会话内有效 | 直接引用 | 保证当前对话连贯性 |
| **中期任务记忆** | 任务全链路产物、决策记录、评审意见 | 任务周期内 | 向量检索 + 关键词匹配 | 支撑任务执行与回溯 |
| **长期知识库** | 通用技能、工作流模板、历史最佳实践、行业知识 | 永久沉淀，自动遗忘 | 向量数据库语义检索 | 跨任务复用经验 |

### 2. 上下文工程控制
- **自动压缩机制**：对话超过阈值时，自动对历史消息做摘要压缩，保留关键决策、任务节点、核心结论，剔除闲聊与冗余内容，token 占用降低 60%+。
- **动态召回**：执行对应任务时，自动召回相关的历史任务、技能模板、领域知识，注入当前上下文，无需人工投喂。
- **遗忘机制**：长期记忆按访问频率、重要度自动评分，低价值记忆定期归档，避免记忆库膨胀。
- **隔离机制**：不同任务、不同租户的记忆完全隔离，数据互不干扰。

### 3. Skill 技能沉淀复用
- 任务验收通过后，自动抽取本次任务的「执行流程、关键方法、prompt 模板、工具组合」，沉淀为标准化 Skill。
- Skill 支持标签分类、版本管理、评分优化；同类任务执行时自动匹配最优 Skill 复用，执行效率持续提升。
- Skill 可导出、可共享、可导入，支持团队级技能库共建。

---

## 六、Agent 行为范式与执行机制
内置三大主流行为模式，不同任务自动匹配最优范式，保证算法健壮性与执行效率。

### 1. Plan-and-Execute 模式
- **适用场景**：复杂任务、多步骤任务、陌生领域任务（如需求拆解、方案设计）。
- **执行流程**：先制定完整执行计划 → 拆分子步骤 → 逐步执行 → 中途校验 → 计划动态调整。
- **优势**：全局把控强，步骤清晰，不易跑偏，适合长链路任务。

### 2. ReAct 模式
- **适用场景**：工具调用型任务、简单执行任务、查询类任务（如代码编写、数据查询、部署操作）。
- **执行流程**：推理→行动→观察→再推理→再行动，循环迭代直至完成。
- **优势**：灵活度高，反馈及时，适合需要动态调整的工具型任务。

### 3. Review 反思模式
- **适用场景**：评审节点、质量校验、方案优化（如代码审查、设计评审）。
- **执行流程**：初步产出 → 自我审查 → 缺陷识别 → 修正优化 → 二次评审。
- **优势**：质量把控强，减少错误流出，降低人类评审成本。

### 4. Agent Loop 自驱机制
- 每个 Agent 独立运行事件循环，自主感知任务、执行任务、上报状态、触发流转，无需中心轮询。
- 支持中断与恢复：任务暂停后恢复执行，上下文完整保留，从中断点继续。
- 资源自适应：高负载时自动降速、低负载时自动并行，保证系统稳定性。

---

## 七、工具与 Function Calling 体系
### 1. 统一工具调用协议
采用标准化 Function Calling 机制，所有工具遵循统一注册、调用、鉴权、返回规范。
- **工具注册**：插件化注册，声明工具名、参数、返回值、权限等级、适用场景。
- **自动发现**：Agent 可根据任务自动匹配可用工具，无需硬编码绑定。
- **幂等保证**：所有写操作工具自带幂等标识，重复调用不产生副作用。
- **失败重试**：工具调用失败自动重试，超时自动熔断。

### 2. 内置基础工具集
| 工具分类 | 核心工具 |
|---|---|
| **文档类** | Markdown 生成、文档模板渲染、PDF 导出、内容总结 |
| **设计类** | 原型生成、图标生成、设计规范检查 |
| **代码类** | 代码生成、代码审查、格式化、编译检查、版本提交 |
| **数据类** | 数据库查询、表格处理、图表生成、指标计算 |
| **系统类** | 文件读写、命令执行、部署发布、日志查询 |
| **信息类** | 网络搜索、知识库检索、信息聚合 |

### 3. 拓展性
- 支持第三方系统接入：Git、Jira、飞书、企业微信、服务器等，通过插件形式对接。
- 支持自定义工具：用户可通过配置快速新增私有工具，接入内部系统。

---

## 八、网页端交互设计（先行落地）
### 1. 整体布局：三栏式群聊工作台
- **左侧：任务导航栏**
  - 全部任务列表、进行中、待我验收、已完成分类
  - 新建任务入口、工作流模板库、技能库入口
- **中间：群聊主区域**
  - 类聊天界面，人类与所有 Agent 同群对话
  - 消息类型：纯文本、任务卡片、产物卡片、评审卡片、审批卡片
  - 支持 @提及、引用回复、任务状态标签
- **右侧：任务详情面板**
  - 当前任务进度可视化（工作流节点高亮）
  - 产物列表、历史记录、评审意见
  - 重大决策投票/审批入口

### 2. 核心交互细节
- **任务发起**：输入框输入任务描述，可选工作流模板，一键发送即启动。
- **过程可视化**：每个 Agent 执行状态实时显示（思考中、执行中、已完成），任务进度条实时更新。
- **人类介入**：关键决策节点自动弹出审批卡片，支持「同意/驳回/修改意见」三种操作。
- **产物预览**：代码、文档、设计图可直接在群聊内预览，无需跳转。
- **追溯回放**：支持任务全链路回放，查看每一步的输入、输出、决策原因。

### 3. 桌面端兼容设计
- 核心逻辑层与 UI 层完全分离，Web 端核心业务逻辑 100% 可复用。
- 后续通过 Tauri 打包桌面端，仅需新增系统级工具（本地文件、系统命令、全局快捷键），UI 小幅适配即可。

---

## 九、技术架构选型（高并发 + 高可维护）
### 前端（Web 端优先）
- **技术栈**：React 18 + TypeScript + Tailwind CSS + Zustand 状态管理
- **架构**：组件化 + 插件化设计，UI 组件、业务组件、功能插件分层
- **通信**：WebSocket 实时消息 + HTTP 接口，保证群聊实时性
- **兼容**：响应式设计，后续桌面端迁移复用 90% 核心代码

### 后端
- **技术栈**：Node.js + NestJS 模块化架构 + TypeScript
- **核心组件**：
  - 任务队列：BullMQ + Redis，支撑高并发异步任务
  - Agent 引擎：统一 Agent 运行时，支持多模型接入
  - 向量数据库：Pinecone / Milvus，支撑记忆与语义检索
  - 关系数据库：PostgreSQL，存储任务、用户、配置、审计日志
- **并发保障**：事件驱动架构 + 连接池 + 限流熔断，支持千级任务并发

### 核心非功能特性
1. **高并发性**：异步队列 + 水平扩展，任务量增长可通过加节点线性扩容。
2. **安全性**：
   - 工具调用权限分级，敏感操作必须人类审批
   - 数据全链路加密，记忆与任务数据租户隔离
   - 全操作审计日志，可追溯可回查
3. **可维护性**：模块化 + 接口化 + 完善日志，问题定位快，修改影响面小。
4. **Prompt 管理**：Prompt 模板化、版本化、可测试、可回滚，不同 Agent、不同场景独立配置。

---

## 十、Agent 评测与运营体系
### 1. 多维度评测集
建立标准化 Agent 评测体系，持续优化效果：
- **任务准确率**：任务完成度、产物合格率、一次通过率
- **执行效率**：任务耗时、步骤数、资源消耗
- **协作能力**：沟通成本、配合顺畅度、冲突解决率
- **成本控制**：token 消耗量、工具调用次数

### 2. 优化机制
- Prompt A/B 测试：同一角色多版本 Prompt 对比，自动择优。
- Skill 迭代：根据评测结果持续优化技能模板，沉淀最佳实践。
- 失败案例库：所有失败任务自动归档，定期复盘优化。

---

## 十一、落地路线图
### Phase 1：Web 端 MVP（核心闭环）
- 实现核心编排引擎 + 调度总管、文档、评审 3 个基础 Agent
- 内置通用工作流模板 + 基础工具集
- 落地群聊式交互界面 + 任务基础管理
- 实现短期记忆 + 基础 Function Calling
- 跑通「人类发任务→拆解→执行→评审→验收」全链路

### Phase 2：能力增强
- 扩展设计、前端、后端、测试等领域 Agent
- 上线自定义工作流 + 技能沉淀体系
- 完善中长期记忆 + 上下文自动压缩
- 新增更多工具集与第三方集成

### Phase 3：体验与拓展
- 上线 Agent 评测体系 + 优化机制
- 强化并发能力与稳定性
- 启动桌面端（Tauri）迁移，打通本地系统能力

---

## 十二、核心优势总结
1. **极致的人类提效**：人类仅管创意与决策，执行层全自动化，生产力提升数倍。
2. **极强的拓展能力**：Agent、工具、工作流全插件化，业务边界可无限延伸。
3. **持续进化的能力**：技能沉淀 + 评测优化，越用越好用，效率持续提升。
4. **平滑的端侧迁移**：Web 先行验证，桌面端无缝承接，架构一次设计多端复用。
5. **工业级稳定性**：异步编排、异常兜底、权限隔离、全链路审计，满足生产级使用。