# P2 施工规划：持久化队列 + 断点恢复 + DAG 建模 + 前端接入

## Context（为什么做）

> ✅ **实施完成 2026-09-15**（含最高强度审查修订）：`pytest` 全量 **84 passed / 1 skipped**、`ruff` 通过、前端 `tsc+vite build` 通过；**双进程实机冒烟通过**（API+ARQ worker + 真实 DeepSeek：入队→worker执行→事件回传→验收卡→审批→收尾 done）。审查🔴项均已落地实现（见"审查修订落地"一节）。

P1 完成了自研工作流（状态机 + 乐观锁 + TaskNode 唯一真相源）与多 Agent 编排，但后台任务全部跑在 **in-process `TaskRunner`**（`asyncio.create_task` + 内存 dict），存在致命限定：**服务重启 / OOM / 崩溃时所有运行中、等待 HITL 的任务全部丢失，无持久化、无重试、无死信**。P1 文档已把此列为 P2 必须解决的风险。

P2 目标：
1. 任务**持久化**（入 Redis 队列 + DB 记录），服务重启不丢，运行中任务 **从 PG TaskNode 断点恢复**。
2. **先单 worker 持久化**（多 worker 并行 + Redis Pub/Sub 跨实例广播留 P3，用户已确认分期）。
3. **DAG 建模 + 串行执行**（`depends_on` 声明与校验，实际仍单活节点顺序跑，不真并行）。
4. **纳入前端**：处理 `agent_message / task_node_update / review_event`，渲染节点状态带 + 审批卡 + 追问卡。

强制约束（延续 P1）：**PG TaskNode 是唯一真相源，内存态不驱动业务**；保留乐观锁与 `AgentRunner.run / run_resume` 现有逻辑；克制、最小必要改动。

## 核心架构决策（已定）

| 决策 | 选择 | 理由 |
|---|---|---|
| 队列框架 | **ARQ**（独立 worker 进程） | async + 纯 Redis，与全异步代码契合；悲观执行天然实现"崩溃重跑" |
| worker↔API 关系 | **独立 ARQ worker 进程 + Redis Pub/Sub 单通道回传** | WS/前端在 API 进程；worker 里 emit 经 `zw:tasks` 通道单点回传 API 推 WS。此为一对一拓扑，**不是** P3 的多实例多对多广播，工作直接铺垫 P3 不浪费 |
| worker 启动兜底 | **DB rescan 重新入队**（`_job_id=task_id` 去重） | 兜住 in-flight job 从 Redis 流意外丢失的边缘情况 |
| DAG | **建模依赖 + 串行执行** | 保持"单活节点"不变式，`engine._activate_next` 只按依赖就绪挑节点，`AgentRunner` 完全复用，改动最小 |
| 断点恢复 | **ARQ 悲观执行 + DB rescan 双保险**；HITL 等待中任务天然靠人工 `user_decision` 重新入队 | 满足"重启不丢 + 待人工"语义 |

## 分阶段实施 + 文件清单

### P2-1 基建升级：依赖 + 模型 + 状态机 + 迁移
- `server/pyproject.toml`：+`arq>=0.28`
- `server/app/config.py`：加 `ARQ_QUEUE_NAME`、`TASK_EVENT_CHANNEL`、`WORKER_ID`、`ARQ_JOB_TIMEOUT(900)`、`ARQ_MAX_TRIES(3)`、`USE_QUEUE(bool)`（`USE_QUEUE=false` 可回滚到 P1 in-process）
- `server/app/db/models.py`：TaskNode 加 `worker_id`、`attempts:int default 0`、`queued_at`、`lease`（JSON）、`depends_on`（JSON）
- `server/app/workflow/state_machine.py`：加 `QUEUED`，`pending→queued→running→done/failed/blocked`
- `server/app/db/repos.py`：加 `set_node_queued`、`claim_node`（乐观锁 running + 写 worker/lease + attempts++）、`release_node`、`scan_running_pending`、`create_node` 接受 `depends_on`
- 新建 alembic 迁移（新增列均可空/带默认，**人工审核后执行**，不清 volume）
- **完成标准**：`make migrate` 成功；状态机/repo 单测通过；`pytest` 全绿

### P2-2 ARQ worker 骨架 + 入队路径
- 新建 `server/app/queue/__init__.py`
- `server/app/queue/arqs.py`：`WorkerSettings`（functions/redis_settings/on_startup/on_shutdown/job_timeout/retry_jobs）、job 函数 `run_agent_task(ctx, task_id)`、`run_agent_resume(ctx, task_id, decision)`、`make_emit(ctx, task_id)`、`create_arq_pool`；`on_startup` 构造 `ctx[session_factory]`、`ctx[registry]`、publish 用 redis 连接；`on_shutdown` 关闭。**job 只传 `task_id:str` + `decision:dict`（msgpack 可序列化，禁传 Pydantic）**
- 把 ws 层的 `_make_registry` 抽为共享 helper（ws 与 worker 共用）
- **完成标准**：`make worker` 能拉取执行一个 mock job；burst worker 单测通过

### P2-3 事件回传 worker→API→WS + WS 改造
- `server/app/queue/events.py`：通道常量、`publish_task_event`、`consume_task_events`（API 侧订阅循环，异常自愈重连）
- `server/app/api/ws.py`：`_submit` 从 in-process submit 改为 `enqueue_job`（`_job_id=task_id` 去重）；新增 `_task_ws`（连接路由，断线时清理）
- `server/app/main.py` + `app/appstate.py`：lifespan 管理 ARQ pool + 事件消费任务 + 关闭回收；`/health` 增 Redis 探测
- `Makefile`：加 `worker` target
- **完成标准**：双进程实机冒烟，WS 收到 worker 发出的 `agent_message/task_node_update/review_event`；`test_ws.py` 更新为入队契约

### P2-4 断点恢复 + DAG 依赖建模
- `server/app/queue/recovery.py`：`resume_inflight(session_factory)`——扫 running/queued/pending 任务；auto 节点任务入队续跑，仅剩 hitl 节点 running 的任务跳过留人工；`enqueue_job(..., _job_id=task_id)` 去重
- `server/app/workflow/templates.py`：`WorkflowNodeSpec.depends_on: list[str] | None = None`；None=依赖模板前一节点，显式值覆盖
- `server/app/workflow/engine.py`：`start` 时依赖校验（未知节点/自引用/成环→拒，`WorkflowStateError`）；`_activate_next` 改为挑「第一个 pending 且 depends_on 全 done」的节点置 running（保持单活）
- **完成标准**：recovery 单测（running-auto 重入队 / hitl 不重入队 / job_id 去重）；DAG 校验单测；手工 kill worker→重启确认续跑

### P2-5 前端接入
- `client/src/lib/protocol.ts`：WsKind 并入 `agent_message | task_node_update | review_event | user_decision` + payload 类型
- `client/src/hooks/useZeffyWs.ts`：加 `sendDecision(taskId, decision)`
- `client/src/App.tsx`：维护 `nodes` 状态（task_node_update 就地更新状态条，颜色映射 pending/queued/running/done/failed）；`agent_message` 渲染领域产物卡片；`review_event` 渲染审批卡（通过/驳回→sendDecision）与追问卡（输入→sendDecision answer）；打开连接时 `fetch(/tasks/{id}/nodes)` 对账弥补断线丢事件（P1 已有 GET 接口）
- **完成标准**：`tsc -b && vite build` 通过；浏览器冒烟可见节点流转、审批卡/追问卡可交互

## 关键复用
- `AgentRunner.run / run_resume`（`app/agents/runner.py`）——worker job 直接调用，**零改动**（从 DB 重建 context，幂等）
- `WorkflowEngine.advance/rewind` 与乐观锁 repos（`app/workflow/engine.py`、`app/db/repos.py`）
- `tests/conftest.py` 内存 SQLite session 夹具
- `GET /tasks/{id}/nodes`（`app/main.py` 已存在）——前端对账

## 测试策略
- DB：内存 SQLite（现有 conftest）
- 队列：`fakeredis[aio]` 起 `ArqRedis` + **ARQ burst worker** 同步跑入队 job；可起真 Redis(6380) 做一条集成冒烟
- 用例：`test_queue.py`（入队→burst 执行/msgpack/job_id 去重）、`test_recovery.py`（DB oracle：running-auto 入队、hitl 不入队、同 task 不重复）、`test_dag.py`（未知/自引用/成环拒；合法依赖下就绪节点激活）；更新 `test_ws.py`、`test_state_machine.py`
- 断点恢复断言：直连 `resume_inflight`，桩掉 `enqueue_job` 记录 `_job_id` 调用即可，无需真起 worker

## 验证（端到端）
1. `make migrate` + `pytest` 全绿
2. 起 PG/Redis 容器 + `make worker` + `uvicorn app.main:app --workers 1`（双进程）
3. WS 冒烟：真实 DeepSeek 提交任务→节点流转→验收卡（`review_event`）→前端审批/追问→收尾
4. 断点恢复：跑一个长任务中途 kill worker→重启 worker→确认任务从 DB 续跑不丢
5. 前端 `tsc -b && vite build` 通过 + 浏览器可见节点状态与卡片

## 风险与回滚
- **R1 ARQ 冲击现有 async**：存量 `_submit` 闭包捕获 `ws` 需改写为入队；`AgentRunner/engine` 核心零改动。回滚=`git revert` P2 源码，`USE_QUEUE=false` 走回 P1 in-process（关入队开关即可）
- **R2 Redis 不可用**：入队抛错→`system_notify`；事件消费自愈重连；`/health` 加 Redis 探测；回滚同上
- **R3 重复执行（悲观执行重跑）**：`run()` 从 DB 重建幂等；fs 工具沿用"存在判断/临时+rename"幂等约定；`_job_id` + `attempts` 双层防重；接受"至少一次"为文档化语义（P3 加 lease 过期扫描）
- **R4 alembic 数据安全**：新增列均 nullable/带默认，autogenerate 产物人工逐行审后方可执行，不清 volume