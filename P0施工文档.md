# P0 · 骨架与基建 施工文档

> 所属：Zeffy-Workplace 作品集 MVP（主文档：`MVP任务文档.md`）
> 目标：**一键起环境 + 跑通"前端发消息 → 后端 echo 返回"的活水链路**，作为 P1 起所有阶段的调试底座。
> P0 原则：**多做"接口预埋/协议标准化/安全壳子"，少做业务**。所有被 P1 大量使用的接口（WS 消息协议、LLMClient、repo、安全函数）P0 就定型为"面向 P1 的签名"，实现可以是简化版，但不写死成 P1 要大改的形状。
> 状态：待审查确认（已吸收第一轮审查意见），尚未编码

---

## 〇、P0 拆分结构（5 个小阶段，各自独立可验收）

```
P0-1 工程初始化
  ├─ P0-2 配置与 LLM 薄封装（LLMClient 面向 P1 签名 + 异常分层 + usage 预埋）
  ├─ P0-3 数据层（5 表 + create_all 警告 + 索引/P1字段注释）
  ├─ P0-4 API 与 WebSocket 服务（JSON 消息协议 + 会话管理壳）
  └─ P0-5 前端骨架 + echo 链路联调（useZeffyWs 钩子 + 重连 → P0 总验收）
```

> 拆分原则：每个小阶段都能"跑起来 + 看得到产出 + 有明确完成标准"，避免 P0 一口气做太多导致无从验收。

---

## 一、P0 总目标与总验收

**一句话**：三步（`docker compose up` + `uv run` + `npm run dev`）起服务，页面发结构化"ping"消息收到"pong"。

**P0 总完成标准（A0 组）**
| # | 验收点 | 门槛 |
|---|---|---|
| A0-1 | 环境可复现 | 全新 clone 后按 README 三步命令能起全部服务（仅需 base compose，不依赖 Qdrant） |
| A0-2 | 数据层可用 | 5 张表建好，健康检查能连 PG；确认 create_all 仅开发用途 |
| A0-3 | LLM 封装可用 | `--self-test` 无 key 走 `LLMConfigError` 分支不崩；有 key 返回文本 + usage 占位 |
| A0-4 | echo 链路 | 前端发 `kind=user_message` 的 JSON，经 WS → 后端 → 前端收到 `kind=system_notify` 的 JSON "pong" |
| A0-5 | 安全底线 | `.env`、`workspace` 不入 git；`safe_resolve_workspace_path` 防穿越函数已实现并有单测 |

---

## 二、P0-1 工程初始化

**目标**：空工作区变成"能装依赖、有目录规范、版本可控"的双包工程。

**新建目录结构**
```
zeffy-workplace/            ← 建议新建为该目录（替换仓库根）
├── docker-compose.base.yml    # PG + Redis（P0 必启）
├── docker-compose.vector.yml  # Qdrant（P1+ 才启用）
├── .gitignore
├── .dockerignore
├── Makefile                # make up / test / lint（可选）
├── README.md
├── server/
│   ├── uv.lock / pyproject.toml
│   ├── app/
│   └── tests/
├── client/
│   ├── package.json / vite.config.ts
│   └── src/
├── .env.example
└── workspace/               ← 工具可写白名单根（gitignore，运行时自动建）
```

**关键操作**
1. `git init`；`uv init server`；`npm create vite@latest client -- --template react-ts`。
   **记录推荐版本到 README**：Python 3.12.x / uv 版本 / vite 版本 / react-ts 版本。
   **锁定版本，禁止 `^` 宽松范围**：`client/package.json` 锁 vite、react-ts 精确版本。
2. **docker-compose 拆分两份**：
   - `docker-compose.base.yml`：PostgreSQL 16 + Redis 7（P0 必启），镜像写固定 tag（`16-alpine` / `7-alpine`），禁止 `latest`。
   - `docker-compose.vector.yml`：Qdrant（P1 才启用）。
   - README 说明：P0 只跑 `docker compose -f docker-compose.base.yml up`。
3. `.gitignore`：排除 `.env`、`workspace/`、`__pycache__`、`node_modules`、`dist` 等。
4. `.dockerignore`：排除 `node_modules`、`.venv`、`workspace` 等，避免未来镜像构建打包体积垃圾。
5. `README.md` 骨架：简介 + 起环境命令占位（联调通过后补全）。
6. `uv add` 依赖（仅 P0 层）：`fastapi uvicorn[standard] langchain-core langchain-openai pydantic-settings sqlalchemy[asyncio] asyncpg`。**Qdrant/LangGraph 留 P1/P2。**

**编码规范约束（新增，防循环导入）**
> 模块依赖方向固定：`api/ws 层 → repo 层`，`repo 层禁止 import api/ws`；`llm 只依赖 config`；`db 只依赖 base 配置`。违反即视为坏味道。

**产物**：双包骨架 + base/vector compose + gitignore + dockerignore + 依赖锁定 + 编码规范说明

**完成标准**：目录可读、依赖解析通过（`uv sync` 与 `npm install` 无红）。

---

## 三、P0-2 配置与 LLM 薄封装

**目标**：配置中心化 + "provider 无关、面向 P1"的模型调用抽象（面试点）。

**新增文件**
- `server/app/config.py`：pydantic-settings，读 `LLM_*`、`DATABASE_URL`、`REDIS_URL`、`QDRANT_URL`（P0 不校验连通）、`PORT`、`WORKSPACE_ROOT`。
- `server/app/llm.py`：`class LLMClient`，封装 LangChain `ChatOpenAI`。
- `server/app/llm_errors.py`：自定义异常分层（见下）。
- `server/app/__init__.py`
- `.env.example`：补全 `PORT / LLM_PROVIDER / LLM_API_KEY / LLM_BASE_URL / LLM_MODEL / DATABASE_URL / REDIS_URL / QDRANT_URL / WORKSPACE_ROOT`

**LLMClient 接口（必须面向 P1，不写死字符串 prompt）**
```python
class LLMClient:
    async def agenerate(
        self,
        messages: list[dict],          # [{"role","content"}, ...] 预埋 P1 多轮/System 消息
        temperature: float | None = None,
        max_tokens: int | None = None,
        stop: list[str] | None = None,
    ) -> tuple[str, dict]:             # 返回 (文本, usage)
        # usage 占位 {"prompt_tokens":0,"completion_tokens":0}，P1 填真实值
```
> P0 实现最小可用；接口签名预埋到 P1 需要的形状，避免重构。**模型配置(全局默认)与运行时参数(可覆盖)分开**，参数不硬编码进 config 单例。

**异常分层（P0 先做壳子，P1 用于重试/降级）**
- `LLMConfigError`（无 key / 配置错误）
- `LLMConnectionError`（连接超时）
- `LLMProviderError`（限流 / 拒绝 / 服务商报错）

**关键操作**
1. 实现 `get_llm()` 单例（懒加载，支持后续多模型壳：注释预留"调度/评审可用不同模型"）。
2. `python -m app.llm --self-test`：输出**结构化结果**（状态/provider/错误信息）而非自由文本，方便自动化调用；**加超时限制**，避免 self-test 卡死 CI。
3. 单测：`test_config`（env 覆盖默认值）、`test_llm_errors`（无 key → `LLMConfigError`）。

**产物**：config 中心 + LLMClient 预埋签名 + 异常分层 + `.env.example`

**完成标准**：`--self-test` 无 key 抛 `LLMConfigError` 且不崩；有 key 返回文本 + usage 占位。

---

## 四、P0-3 数据层

**目标**：5 张表能建、能连、能查；并为 P1 预埋字段/索引注释。

**新增文件**
- `server/app/db/base.py`：`Base(DeclarativeBase)` + async engine + session factory
- `server/app/db/models.py`：Task / Message / TaskNode / AuditLog / EvalRun
- `server/app/db/init_db.py`：首启建表脚本
- `server/app/db/repos.py`：极简仓储层 + 异常包装

**必须遵守的建表策略（重要警告）**
> ⚠️ `metadata.create_all` **仅限 P0 本地开发**：它只建不存在的表，P1 若加字段不会自动同步，会引发运行时缺列 bug。**P1 阶段必须切换 alembic 迁移，废弃 create_all。** README 注明：P0 调试改模型后，用 `docker compose -f docker-compose.base.yml down -v` 清 volume 重置数据库。

**模型约束**
- **时间字段统一 UTC**：所有表含 `created_at: datetime(utc)`、`updated_at: Optional[datetime(utc)]`，禁止本地时区。
- **字段分级注释**：P0 实现的最小字段，其余标 `# P1-待实现` / `# P2-待实现`，禁止 P0 写未使用的业务字段。
- **索引规划注释（P0 不建，P1 建）**：`Message.task_id`、`TaskNode.task_id`、`AuditLog.created_at`。
- `TaskNode` 是 P1 核心对象，P0 **仅 ORM 壳，不实现流转逻辑**。

**新增文件：`server/app/db/security.py`（安全壳子，P0 就实现并测）**
```python
def safe_resolve_workspace_path(relative_path: str) -> Path:
    """拼接 WORKSPACE_ROOT + relative_path，防 ../ 穿越；越界抛异常。P1 工具唯一入口。"""
```
> 这是后续 Agent 文件工具最重要的安全屏障，P0 写好避免 P1 临时漏写造成目录穿越漏洞。

**关键操作**
1. 定义 5 个 Async ORM + 外键关联 + 时间字段 + 注释。
2. `init_db.py` 幂等建表；启动时检查 `WORKSPACE_ROOT` 存在，不存在递归创建并校验读写权限，失败抛异常。
3. `repos.py` 统一异常包装（捕获 DB 异常 → 抛业务异常，不把 SQLAlchemy 底层异常漏到 API 层），提供 `create_task/get_task/list_by_status`。
4. `security.py` 实现 + 单测（含 `../` 穿越、绝对路径越界）。

**测试**：`test_db` 用**内存 SQLite**（不依赖 docker PG 提速）；`test_security` 防穿越。

**产物**：5 表模型 + create_all 警告 + repo + 安全函数 + 单测

**完成标准**：内存 SQLite 建表写读通过；防穿越函数单测通过。

---

## 五、P0-4 API 与 WebSocket 服务

**目标**：后端有可用 HTTP + WS 入口，WS **自 P0 起就用结构化 JSON 协议**（对齐 P1 群聊）。

**新增文件**
- `server/app/main.py`：FastAPI app，lifespan 管理 engine
- `server/app/api/schemas.py`：Pydantic 请求/响应 schema（`POST /tasks` 入参出参已定义，不裸 dict）
- `server/app/wsmessage.py`：WS 消息协议枚举 + 结构
- `server/app/api/ws.py`：WS 端点 + 会话管理壳
- `server/app/utils/version.py`：返回项目版本号

**WS 消息协议（P0 就定型，P1 直接复用）**
```json
{
  "msg_id": "uuid-str",
  "kind": "user_message | agent_reply | system_notify | task_update | review_notify",
  "payload": "string or object",
  "task_id": "uuid|null",
  "timestamp": "utc iso string"
}
```
> P0 的 echo：收 `kind=user_message` payload="ping" → 回 `kind=system_notify` payload="pong"。**放弃裸文本**。kind 枚举已预埋 P1 的 `agent_message/task_update/review_notify`。

**WS 会话管理壳**
- 记录活跃连接集合（内存级），带 `connection_id`；注释写明：**仅演示用途，P1 迁移 Redis 管理、P2 支持多实例广播**。

**关键操作**
1. `/health` 返回 `{status, db, version}`，DB 断开降级而非 500。
2. `/ws` 收发统一走上面的 JSON 协议；**捕获客户端断开异常清理连接，防泄漏**。
3. CORS：开发环境允许 `localhost:*`；注释写明生产环境改为读配置的允许源，禁止通配符上线。
4. `POST /tasks`：P0 仅落库骨架，输入输出用 Pydantic schema，P1 直接扩展。
5. **开发强制单 worker**：`uvicorn --workers 1`（README 写明；P1 引 Redis Pub/Sub 解决多 worker 广播）。

**测试（集成用 websockets 客户端）**
- `test_api`：httpx 打 `/health`、`/tasks`。
- `test_ws`：websockets 客户端自动化跑通 ping→pong，减少人工。

**产物**：FastAPI 入口 + health + JSON WS 协议 + 会话壳 + schema + 单测/集成测试

**完成标准**：`/health` 200 且 DB=true + 版本号；WS json ping→pong（自动化测试通过）。

---

## 六、P0-5 前端骨架 + echo 链路（P0 总验收）

**目标**：全部打通，跑通 A0-4，README 命令验证可复现。

**新增/修改文件**
- `client/src/hooks/useZeffyWs.ts`：**抽离 WS 钩子**（不把 WS 写在 UI 组件内），封装连接/收发/重连/待发队列。
- `client/src/App.tsx`：仅最小消息列表 + 输入框（`kind=user_message`），不做 UI 美化（P4 统一做）。
- `client/src/lib/protocol.ts`：前端侧的 WS 消息协议类型与构建函数（对齐后端 schema）。
- 修改 `client/vite.config.ts`：代理 `/api`、`/ws`（**注意 `ws:true` 开关，很多人在此踩坑**）。

**关键操作**
1. 前端发 `type user_message` 的 JSON，展示收到的 `system_notify`。
2. WS 断线重连：**指数退避**，不无限高频重连；本地缓存未发送消息队列，重连成功自动补发（P0 做基础版）。
3. 联调 CORS/代理，npm run dev 下 WS 走通。
4. 按 README 全新建 + 起环境，验证 A0-1~A0-5。

**产物**：useZeffyWs 钩子 + 协议模块 + 极简 echo 前端 + 完整 README

**完成标准**：A0-1~A0-5 通过；提供阶段变更摘要（含遗留缺口清单）。

---

## 七、P0 风险与回滚（含审查补充）

| 风险 | 影响 | 预案/回滚 |
|---|---|---|
| uv/Vite 版本漂移 | 脚手架失败 | 锁定精确版本到 package.json + README；失败改最小手写配置 |
| asyncpg+SQLAlchemy 2.0 | 连池报错 | 单测走内存 SQLite，PG 联调按需；失败先用同步引擎 |
| 前端 WS 代理跨域 | 联调不通 | 先直接连服务端口验证，代理后加回归（勿忘 `ws:true`） |
| LLM key 缺失 | self-test 崩溃 | `LLMConfigError` 分支友好返回；加超时防卡死 |
| WS 多 worker 断开 | 长连接不稳 | P0 强制 `--workers 1`；README 写明，P1 引 Redis Pub/Sub |
| PG volume 残留旧数据 | 表变更后异常 | README 提示 `docker compose -f docker-compose.base.yml down -v` 重置 |
| workspace 挂载权限冲突 | docker 容器读写问题 | README 提示 Windows/mac/Linux 挂载注意事项 |
| `create_all` 不更新字段 | P1 缺列 bug | 明确切 alembic；P0 改模型须清 volume |

---

## 八、P0 测试体系

**三层测试划分（立项为规范）**
1. **单元测试（pytest）**：不依赖 docker —— 内存 SQLite、mock LLM。覆盖 `test_config / test_llm_errors / test_db / test_security`。
2. **集成测试（pytest + websockets）**：依赖 base compose，覆盖 `test_api`（httpx）、`test_ws`（ping→pong）。
3. **手工验收（A0-1~A0-5）**：README 复现清单。

> P0 不做性能/并发压测（P2 引入）。

---

## 九、P0 为 P1 的预埋清单（最高价值点）

| 预埋项 | 完成位置 | P1 直接复用 |
|---|---|---|
| WS JSON 消息协议（kind 枚举含 agent/task/review 占位） | P0-4/P0-5 | 群聊 @、agent 消息、任务通知 |
| LLMClient messages 参数 + usage 返回 + 异常分层 | P0-2 | 成本统计、重试降级 |
| DB 模型 P1/P2 字段 + 索引注释 | P0-3 | 避免改表 |
| `safe_resolve_workspace_path` 防穿越 | P0-3 | Agent 文件工具唯一入口 |
| repo 统一仓储模式 | P0-3 | 新增业务全走 repo，杜绝裸 SQL |
| `useZeffyWs` 钩子 + 指数退避重连 | P0-5 | 群聊前端 |
| **P0 变更摘要中的「遗留缺口清单」** | 阶段末 | 直接作为 P1 施工文档输入 |

**P0 明确遗留缺口（写进变更摘要）**：无 Agent 运行时 / 无任务编排与 TaskNode 流转 / Qdrant 未接入 / 无记忆系统 / WS 仅单实例内存会话 / 无权限与用户体系（**单用户本地 MVP，鉴权 P2 再做，刻意维持现状**）。

---

## 十、审查意见处置表（review → 处置）

| # | 意见 | 处置 | 说明 |
|---|---|---|---|
| 必改-1 | compose 拆 base/vector，P0 不启 Qdrant | ✅ 吸收 | base=PG+Redis / vector=Qdrant(P1) |
| 必改-2 | WS 全用结构化 JSON 协议，弃裸文本 | ✅ 吸收 | wsmessage.py + kind 枚举 + 前端 protocol.ts |
| 必改-3 | LLMClient 不写死字符串，预埋 messages/usage | ✅ 吸收 | agenerate(list[dict]) -> (text, usage) |
| 必改-4 | create_all 仅 P0，P1 切 alembic | ✅ 吸收 | 文档+README双警告 |
| 必改-5 | safe_resolve_workspace_path 防穿越 | ✅ 吸收 | security.py + 单测 |
| 强-1 | 锁定脚手架/docker 版本 | ✅ 吸收 | 精确 tag + 非 `^` 版本 |
| 强-2 | 时间统一 UTC + 索引/字段注释 | ✅ 吸收 | 模型约束 |
| 强-3 | LLM 自定义异常分层 | ✅ 吸收 | llm_errors.py |
| 强-4 | useZeffyWs 钩子 + 指数退避重连 | ✅ 吸收 | P0-5 |
| 强-5 | 变更摘要带遗留缺口清单 | ✅ 吸收 | 第九节 |
| 强-6 | ruff + mypy 预埋 | ✅ 吸收 | 见下「静态检查」 |
| 可选-1 | Makefile / dockerignore | ✅ 吸收 | P0-1 |
| 可选-2 | 内存 sqlite 跑单测 | ✅ 吸收 | P0-3 |
| 新增-1 | 模块依赖边界防循环导入 | ✅ 吸收 | P0-1 编码规范 |
| 新增-2 | 变更摘要标准化模板 | ✅ 吸收 | 见下「变更摘要模板」 |

**静态检查预埋（强-6）**
- `server/pyproject.toml` 增加 `[tool.ruff]`、`[tool.mypy]` 配置；命令 `uv run ruff check`、`uv run mypy`。
- P0 本地手动执行即可；**CI 预埋到 P1**。

**变更摘要模板（新增-2，每阶段沿用）**
```
新增文件列表 / 修改文件列表
关键行为变更
全部验收结果（对应用例/手工项）
遗留缺口 & P1 承接点
已知限制 & 本地复现注意事项
```

---

## 十一、P0 测试与验收清单

- [ ] P0-1：`uv sync` + `npm install` 无红；ruff/mypy 本地通过；版本已锁定
- [ ] P0-2：`--self-test` 无 key 抛 `LLMConfigError`；有 key 返回文本+usage
- [ ] P0-3：内存 SQLite 建表写读通过；`test_security` 防穿越通过
- [ ] P0-4：`/health` 200/DB=true/version；`test_ws` 自动跑通 json ping→pong
- [ ] P0-5：A0-1~A0-5 全过，生成含**遗留缺口清单**的 P0 变更摘要

> 完成 P0 后提交：**P0 变更摘要 + P1 拆解版施工文档**（P1 条目化小阶段，供你审查后进入 P1）。

---

## 十二、说明：范围不变，核心仍是最小活水链路

本次修订**不改变 P0 的目标与工作量量级**（仍是一键起环境 + echo 链路），所有吸收项都属于"接口预埋 / 协议标准化 / 安全壳子 / 规范加固"，未引入 P0 阶段用不到的 Qdrant 或 LangGraph。若你认为某项（如 ruff+mypy 或 Makefile）在 P0 属过度投入，可标注回退为「可选延后」，不阻塞本阶段交付。